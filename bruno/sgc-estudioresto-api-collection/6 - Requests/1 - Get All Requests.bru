meta {
  name: 1 - Get All Requests
  type: http
  seq: 1
}

get {
  url: {{base_url}}/restaurantRequest/requests
  auth: inherit
}

tests {
  test("Returns 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Response is an array", function() {
    expect(res.body).to.be.an("array");
  });
}
